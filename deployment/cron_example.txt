# Run screener once every 5 minutes (one-shot mode)
# Replace /path/to/ultimate-screener with actual project path.
*/5 * * * * cd /path/to/ultimate-screener && .venv/bin/python src/main.py --once >> logs/cron.log 2>&1
