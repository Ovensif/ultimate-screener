# Run screener once every 5 minutes (one-shot mode)
# Replace /path/to/crypto_futures_screener with actual project path.
*/5 * * * * cd /path/to/crypto_futures_screener && python3 src/main.py --once >> logs/cron.log 2>&1
